# Scenario (trusted operator note — not part of the customer's message)

This is a genuinely first invocation: `.internal/onboarding-state.json`
does not exist yet in this workspace. The repo is an unmodified clone of
the Assets Hub Spark template. Treat the user prompt below exactly as
written; do not assume any prior conversation or session state exists.
